# Flexible-Columns-FlexCSS-CodingDojo
## Utilizando lo que hemos aprendido sobre la propiedad flex, vuelve a crear el siguiente sistema de columnas creando las siguientes clases de CSS:

* ```.row```
* ```.col```
* ```.col-2```
* ```.col-3```
